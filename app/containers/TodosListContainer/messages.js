/*
 * TodosListContainer Messages
 *
 * This contains all the text for the TodosListContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TodosListContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'to-do list',
  },
});
