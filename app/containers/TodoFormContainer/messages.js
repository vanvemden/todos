/*
 * TodoFormContainer Messages
 *
 * This contains all the text for the TodoFormContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TodoFormContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TodoFormContainer container!',
  },
});
